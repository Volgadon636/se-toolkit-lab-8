[project]
name = "nanobot"
version = "0.1.0"
description = "LMS nanobot agent"
requires-python = ">=3.11"
dependencies = [
    "nanobot-ai>=0.1.4",
    "lms-mcp",
]

[tool.uv.sources]
lms-mcp = { path = "../mcp", editable = true }
